# Data layer — Prisma, Server Actions, JSON boundary

Read this for: schema/model questions, adding a column, anything touching `src/actions/` or `src/lib/db/`.
Sibling files: [llm-runtime.md](llm-runtime.md), [app-surface.md](app-surface.md), [desktop-tauri.md](desktop-tauri.md).

## The hard rule

**Server = database only. LLM = client only.**

`src/actions/*` are `'use server'` files that do **only** Prisma/SQLite CRUD. No REST route handlers, no
server-side `fetch` to internal endpoints, no LLM call ever runs on the server. There is no `src/app/api/`
directory and adding one is a design violation, not a shortcut.

## Models (`prisma/schema.prisma`)

SQLite via Prisma. `generator client` outputs to `node_modules/.prisma/client`; `datasource db` takes its
URL from the environment (`DATABASE_URL`), which is why the dev DB and the packaged desktop DB can differ
(see [desktop-tauri.md](desktop-tauri.md)).

Every model is snake_case in SQLite via `@map`, camelCase in TypeScript.

| Model            | Table              | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| ---------------- | ------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `Profile`        | `profiles`         | The base CV. Multi-profile: `label` distinguishes them. Scalar contact fields (incl. `headline`) + **10 JSON string columns** (see below). EU/German CV fields: `photo` (base64 data URL), `nationality`, `dateOfBirth`, `hobbiesJson`, plus `workAuthorization`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `Company`        | `companies`        | Employer info (`industry`, `marketPosition`, `locationCity`/`locationCountry`). One-to-many with `Job`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `Contact`        | `contacts`         | Recruiter details. Optional 1:1 with `Job`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `Job`            | `jobs`             | Central row. `status` is a **string constrained at the application level**, not a DB enum — see `JOB_STATUSES` in `src/types/job.ts`. `jobDetailsJson` holds parsed JD. Optional `url` (deduped by `findJobByUrl`). `hiddenAt` (nullable `DateTime`) is a dashboard **view preference, not a status** — non-null means hidden from the `/` grid via `setJobHidden`; hidden jobs still count toward the stat cards.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `Resume`         | `resumes`          | `contentJson` = `ResumeJSON`. Required 1:1 `Customization`. Has many `ResumeSnapshot`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `ResumeSnapshot` | `resume_snapshots` | Version history — `contentJson` + `label`, indexed on `resumeId`. Powers `HistoryDrawer.tsx`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `CoverLetter`    | `cover_letters`    | `contentText` is **plain text, not JSON** — unlike `Resume`. Required 1:1 `Customization`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `Customization`  | `customizations`   | Per-document styling. Legacy scalar columns (`template`, `fontSize`, `pageFormat`, `fontFamily`, `lineHeight`, `marginSize` as a CSV of inches, `colors` as a CSV, `background`, `dateFormat`, `fitToPage`) **plus** `themeJson`. `themeJson == null` means "derive from the legacy scalars via `legacyToTheme()`" — do not assume it is populated. `coverLetterTemplate` (nullable) is the cover letter's own template id, independent of `template` (the resume's); null means "not chosen yet, derive from `template`" — see [rendering.md](rendering.md).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `TokenUsage`     | `token_usage`      | Analytics. `id` is a uuid string (every other model is autoincrement Int). Cost stored as `costMicrocents` (USD × 1e6) to stay integer. Indexed on provider/model/createdAt/purpose/requestId.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `ATSAnalysis`    | _(unmapped)_       | `contentJson` = `ATSAnalysisJSON`. The only model **without** an `@@map`, so its table is `ATSAnalysis`. Attached to both `Job` (as `baseProfileAnalysis`) and `Resume`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `FitCheck`       | _(unmapped)_       | `contentJson` = `FitCheckJSON`. Mirrors `ATSAnalysis`'s shape exactly, including being unmapped. Optional 1:1 on **both** `Job.fitCheckId` (bookmark-stage quick check, run against the base `Profile` before a `Resume` exists — `saveFitCheckForJob`) and `Resume.fitCheckId` (in-editor check, run against the tailored resume — `saveFitCheck`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `EmailAccount`   | `email_accounts`   | One connected Gmail inbox (`provider` currently only `"GMAIL"`, `email` @unique). The `accessToken`/`refreshToken`/`tokenExpiry` columns exist but are **not actively written** — tokens live client-side in `keyStorage` (see [llm-runtime.md](llm-runtime.md)). The columns stay in the schema, unused, because `scripts/migrate-app-db.mjs` is additive-only (see hard rule 5); an install that connected on an older build may still carry plaintext tokens here, wiped once via `wipeLegacyPlaintextTokens()` (called from `useEmailSync`'s mount effect).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `JobEmail`       | `job_emails`       | One synced recruiting email. Optional FKs to `Job`/`Company` (`onDelete: SetNull`, so deleting the job keeps the email as unmatched rather than deleting it); `jobMatcher.ts` does the matching. `stage`/`confidence`/`nextSteps`/`actionRequired` come from `src/lib/llm/emailClassifier.ts` (client-side LLM call, consistent with hard rule 1 — see [llm-runtime.md](llm-runtime.md)). `companyName`/`role` are the classifier's extraction (the sender is often a relay like LinkedIn, so the card shows `companyName ?? extractCompanyName(row) ?? sender` — `src/lib/email/companyName.ts`; the fallback covers rows synced before the columns existed). `hiddenAt` (non-null = hidden from `/emails`) is a view preference like `Job.hiddenAt` — it never affects matching or job status. `kind` is `APPLICATION` (default) or `ALERT` — a job-board digest (LinkedIn/Indeed/StepStone "jobs for you"), which is never matched to a job, never changes a job's status, and is excluded from `/emails` and its counts; its postings are the `JobListing` rows below. |
| `JobListing`     | `job_listings`     | One posting extracted from an `ALERT` digest (`onDelete: Cascade` from its `JobEmail`). `url` is **unique and normalized** (`src/lib/email/listingUrl.ts` strips tracking params and LinkedIn's `/comm/`) — it is the dedupe key across digests. `hiddenAt` = dismissed (view preference). There is no `savedJobId`: "saved" is derived in `getJobListings` by matching normalized `Job.url`, so it stays right for jobs saved via `/bookmarks` or Find Jobs.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |

### JSON string columns

SQLite has no JSON type here — these are `String` columns holding serialized JSON. Parse/stringify **at the
Server Action boundary** so callers only ever see typed objects.

- `Profile`: `skillsJson`, `experienceJson`, `projectsJson`, `educationJson`, `certificationsJson` (all
  `@default("[]")`, non-null) and `publicationsJson`, `languagesJson`, `volunteerJson`, `awardsJson`,
  `hobbiesJson` (nullable — absent means the section was never filled).
- `Job.jobDetailsJson` → `JobDetailsJSON`; `Resume.contentJson` / `ResumeSnapshot.contentJson` → `ResumeJSON`;
  `ATSAnalysis.contentJson` → `ATSAnalysisJSON`. All defined in `src/types/resume.ts`.

A `Profile`'s content is structurally a `ResumeJSON` too — that is what lets `applyResumeOps()` edit a profile
(the MCP profile-edit trio, see `.claude/knowledge/chat-mcp.md`).

**Stored `ATSAnalysis.contentJson` rows predate the current schema.** Rows written before v1.14 still carry
fields `ATSAnalysisSchema` no longer declares (`missing_keywords`, `formatting_issues`, `match_status`, three
of the four `scores`, `estimated_score_delta`, `title_alignment.resume_title`/`.target_title`). Zod strips
unknown keys, so they parse fine and the stripped fields simply vanish — never read one back expecting it to
be there, and never re-add a field name assuming old rows carry a usable value. `knockout_risks` and
`title_alignment` keep object-level `.default()`s precisely because rows exist that predate them. The
`ATSAnalysisSchema back-compat` suite in `src/types/resume.test.ts` pins this; extend it rather than
replacing it when the schema changes again.

### Cascades

Nearly every relation is `onDelete: Cascade`, so deleting a `Job` takes its `Resume`, `CoverLetter`, `Contact`,
and `ATSAnalysis` with it. `Job.profile` is the exception (no cascade) — deleting a `Profile` does not delete
its jobs.

## Server Actions (`src/actions/`)

| File              | Exports                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| ----------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `job.ts`          | `createJob`, `attachGeneratedMaterials`, `createResume`, `getAllJobs`, `getJobById`, `getJob`, `updateJobStatus`, `setJobHidden`, `deleteJob`, `deleteJobById`, `deleteBookmarksOlderThan`, `getResumeByJobId`, `updateResume`, `getResumeSnapshots`, `restoreResumeSnapshot`, `saveAtsAnalysis`, `saveFitCheck`, `saveFitCheckForJob`, `getCoverLetterByJobId`, `updateCoverLetter`, `updateOrCreateCustomization`, `findJobByUrl`, `getAllDocuments`, `getAllJob`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `profile.ts`      | `getAllProfiles`, `getProfileById`, `createProfile`, `updateProfile`, `deleteProfile`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `tokenUsage.ts`   | Token-usage persistence, written by `src/lib/llm/tokenTracker.ts`, read by `/analytics/tokens`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `backup.ts`       | Full-database JSON export/import behind Settings → "Backup & Restore". **Excludes API keys** — they never live in SQLite (see [llm-runtime.md](llm-runtime.md)). Covers `FitCheck` (jobs/resumes FK to it); `fitChecks` is optional on import — an older backup without it has its `fitCheckId`s nulled instead of failing on the FK. Does **not** cover `EmailAccount`/`JobEmail`. The restore ships the whole JSON as one Server Action arg, so `next.config.ts` raises `serverActions.bodySizeLimit` (default 1MB silently breaks real backups with a thrown, empty-`{}` client error). Tested in `backup.test.ts`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `urlFetcher.ts`   | Server-side fetch of a job-posting URL (this is fetching an _external_ page, which is allowed — the ban is on server→internal-endpoint calls and server-side LLM). Tested in `urlFetcher.test.ts`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `getServerUrl.ts` | Resolves the running server's own base URL (differs dev vs. bundled desktop).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `emailSync.ts`    | DB-only actions backing the email tracker: `getEmailSyncStatus` (DB presence only — `useEmailSync` combines it with a client-side token check for "Connected"), `upsertEmailAccount`, `getSyncCursor`, `filterNewMessageIds`, `persistClassifiedEmails` (matches + writes `JobEmail` rows and applies job-status transitions, one row at a time so one bad row doesn't abort the batch), `consumeAuthCode`, `disconnectGoogleAccount`, `wipeLegacyPlaintextTokens`, and the `/emails` page's `getAllTrackedEmails` (returns hidden rows too — the page filters client-side; exports `TrackedEmail`), `setJobEmailHidden`, `linkEmailToJob`, `unlinkEmail` (all `APPLICATION`-only: `getAllTrackedEmails` and the status counts filter on `kind`). For `/opportunities`: `getJobListings` (returns each row with a derived `savedJobId`; exports `JobListingRow`) and `setListingHidden`. `getSyncCursor` also returns `alertsAfter` (newest stored `ALERT` row; null = backfill the 28-day window), and `getMisfiledAlerts` / `promoteStoredAlerts` convert unlinked pre-`kind` digests (detected by the pure `isJobAlert` in `src/lib/email/jobAlert.ts`, which is safe to import from actions). `persistClassifiedEmails` takes an optional `listings[]` per row and, for `kind: "ALERT"`, skips matching entirely and inserts only postings whose normalized `url` is new. Fetching Gmail, OAuth token exchange, and LLM classification all run client-side (`src/lib/email/{gmailClient,connectGmail,runEmailSync}.ts`, `src/lib/llm/emailClassifier.ts`, `listingExtractor.ts`) — see the hard-rule-1 exception in `CLAUDE.md` and [docs/EMAIL_TRACKING.md](../../docs/EMAIL_TRACKING.md). |

`createJob` vs `attachGeneratedMaterials`: `createJob` makes a new `Job` row; `attachGeneratedMaterials`
attaches a resume/cover letter/ATS analysis to an **existing** row and flips its status — that is the bookmark
promotion path (see [app-surface.md](app-surface.md)).

`deleteBookmarksOlderThan(days, profileId?)` bulk-deletes stale rows; its `status: "BOOKMARKED"` filter is
load-bearing — it's what keeps a tracked application (any other status) from ever being caught by the cleanup.
`src/lib/jobs/fitLevel.ts`'s `resolveFitLevel()`/`parseFitLevel()` centralize parsing a persisted `FitCheck`
blob into a `FitLevel` (preferring the in-editor resume-level check over the bookmark-stage job-level one) —
deliberately `prisma`-free so client components (e.g. the dashboard's fit filter) can import it too.

`src/lib/db/job.ts` holds the non-`'use server'` query helpers the actions and the MCP server share
(`src/mcp/db.ts` reaches these through `McpDeps`), tested in `src/lib/db/job.test.ts`.
`src/lib/prisma.ts` is the singleton client.

## Changing the schema

1. Edit `prisma/schema.prisma`.
2. `npm run db:generate` then `npm run db:push`.
3. **Also update `scripts/migrate-app-db.mjs`** if installed desktop apps must carry the change forward — a
   `db push` only touches the local `dev.db`, never an existing user's `$APPDATA/app.db`. This step is the one
   most often forgotten; see [desktop-tauri.md](desktop-tauri.md).
